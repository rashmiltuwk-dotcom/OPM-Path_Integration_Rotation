function FrameCallback(source, eventData)
    % Inspect eventData structure
    disp('eventData fields:');
    disp(fieldnames(eventData));
    
    data = eventData;  % Try using eventData directly for now
